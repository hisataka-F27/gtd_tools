# みなも — 改善ロードマップ

2026-08-30 時点。リファクタリング完了（main `543055e` / 118 tests）後、
「使い勝手を上げる」観点で洗い出した改善案を、優先順位付きで記録する。

優先度の意味:

| | 意味 |
|---|---|
| **P0** | 最優先。次に着手する。実行計画は [PLAN-P0.md](PLAN-P0.md) |
| **P1** | P0 の次。着手時に別途計画を書く |
| **P2** | やる価値はあるが急がない。手が空いたときに拾う |
| **P3** | 課題として検出済み。当面は対応しない（放置の判断を明示的に残すためここに置く） |

番号（#1 等）は洗い出し時の通し番号で、優先度とは無関係。以降の会話でもこの番号で参照する。

---

## P0 — 最優先

（現在なし。P0 として洗い出していた #1 / #2 / #10 はすべて対応済み。次の洗い出しまで空。）

---

## P1 — P0 の次

### #6 「今日やる」の印
次のアクションが増えると、朝に「今日はこれをやる」と決めて置く場所がない。
`flagged` フラグと専用ビューを足す。GTD 原典からはやや外れるが、実運用では効く。

### #12 レビュー中にその場で処理する
週次レビューで「開く」を押すとオーバーレイが閉じ、レビューが中断する
（[30-actions.js:269](src/js/30-actions.js:269) `openFromReview`）。
行内で 完了 / いつかへ送る / 日付変更 ができれば、レビューが一周で終わる。

---

## P2 — 次点

### #3 収集欄で複数行ペースト → 1行1項目
`capture()` は入力欄の値を丸ごと1項目にする（[90-app.js:6](src/js/90-app.js:6)）。
議事録やメモから10行貼って一気にトレイへ、ができると収集のコストが下がる。

### #5 完了・状態変更のフィードバック
定型カードには `flash()` があるが（[14-view-routines.js:1](src/js/14-view-routines.js:1)）、
一覧のチェックは黙って消えるだけ。#2 のトースト基盤に乗せられる。

### #8 完了項目のアーカイブ
`done` は無限に貯まる。localStorage の容量にも、完了ビューの見通しにも効く。
一定日数より古いものを畳む／書き出して削除する。

### #11 レビュー履歴の表示と書き出しの催促
`db.review.history` は26件溜めているのにどこにも表示されていない
（[30-actions.js:264](src/js/30-actions.js:264)）。レビュー完了画面に履歴と
「最後に書き出した日」を出し、古ければ書き出しを勧める。

---

## P3 — 検出済み・当面は対応しない

対応しないと決めたわけではなく、「今は優先しない」もの。再検討のきっかけになる条件を併記する。

### #4 全ビュー横断の検索
`ui.q` はグローバルだが、絞り込むのは今開いているビューの中だけ
（[12-view-list.js](src/js/12-view-list.js)）。「あれどこに入れたっけ」が探せない。
→ 項目数が数百件規模になったら再検討。

### #7 待ちの督促日
現状は `since` からの経過日数だけで、7日経つと自動的にレビューに載る
（[05-query.js](src/js/05-query.js) `oldWaiting`）。「この件は来週まで待つ」を明示できない。
→ レビューで「まだ待ってよい」ものが毎回並ぶようになったら再検討。

### #9 完了起点の繰り返し定型
今の定型は周期が来たら投入する方式（[06-routines.js](src/js/06-routines.js)）。
「終わってから2週間後」型（散髪、掃除など）は表現できない。
→ そういう定型を登録したくなったら再検討。

---

## 対応済み

| | 内容 | コミット |
|---|---|---|
| — | 単一 HTML 1430行 → `src/` 分割 + `build.js` + テスト | `refactor/split-sources` |
| — | かな漢字変換の確定 Enter で項目が追加される不具合 | `fix/ime-enter` |
| — | プロジェクトへの行動追加後にフォーカスが戻らない | `fix/padd-refocus` |
| — | `#eTpl` / `#fBack` ほか未ガード参照で例外が出る | `fix/null-guards`, `fix/null-guards-rest` |
| #10 | 世代バックアップ（起動時1日1回 `.bak` 退避 + 設定パネルから復元） | `feat/backup` |
| #2 | 取り消し（Undo）— db スナップショットを最大10件保持し、トーストと `Cmd/Ctrl+Z` で戻せる | `feat/undo` |
| #1 | キーボード操作 — `j`/`k` でカーソル移動、`Enter` で開く、`x` で完了トグル、`e` で明確化 | `feat/keyboard-nav` |
